{
  "rules": {
    "publicUsernames": {
      ".read": true,
      ".write": "auth !== null"
    },

    "users": {
      ".read": true,
      "$uid": {
        ".write": "auth.uid === $uid",
        "username": { ".write": "auth.uid === $uid", ".read": true},
        "profilePictureURL": { ".write": "auth.uid === $uid", ".read": "auth.uid === $uid" },
        "email": { ".write": "auth.uid === $uid", ".read": true },
        "lastUsernameChange": { ".write": "auth.uid === $uid", ".read": "auth.uid === $uid" },
        "chatsIn": { ".write": "auth !== null", ".read": "auth.uid === $uid"},
        "blockList": { ".write": "auth.uid === $uid", ".read": "auth.uid === $uid" }
      },
      ".indexOn": ["username", "email"]
    },

    "members": {
      "$chatID": {
        ".read": "auth !== null && root.child('members/' + $chatID + '/' + auth.uid).exists()",
        ".write": "auth !== null && (root.child('members/' + $chatID + '/' + auth.uid).exists() || root.child('chats/' + $chatID + '/owner').val() === auth.uid)",
        "$uid": {
          ".read": "auth !== null && root.child('members/' + $chatID + '/' + auth.uid).exists()",
          ".write": "
            auth !== null &&
            (
              ($uid === auth.uid && newData.hasChildren(['isBanned', 'isRemoved', 'username', 'isOnline', 'profilePictureURL']) &&
              newData.child('isBanned').isBoolean() &&
              newData.child('isRemoved').isBoolean() &&
              newData.child('isOnline').isBoolean() &&
              newData.child('username').isString() &&
              newData.child('profilePictureURL').isString() &&
              (!data.exists() || (
                newData.child('username').val() === data.child('username').val() &&
                newData.child('profilePictureURL').val() === data.child('profilePictureURL').val() &&
                newData.child('isBanned').val() === data.child('isBanned').val() &&
                newData.child('isRemoved').val() === data.child('isRemoved').val()
              ))) ||
              (root.child('chats/' + $chatID + '/owner').val() === auth.uid &&
              newData.hasChildren(['isBanned', 'isRemoved', 'username', 'isOnline', 'profilePictureURL']) &&
              newData.child('isBanned').isBoolean() &&
              newData.child('isRemoved').isBoolean() &&
              newData.child('isOnline').isBoolean() &&
              newData.child('username').isString() &&
              newData.child('profilePictureURL').isString())
            ) ||
            (!data.exists() && !newData.exists())
          "
        }
      }
    },

    "messages": {
      "$chatID": {
        ".read": "auth !== null && root.child('members/' + $chatID + '/' + auth.uid).exists()",
        ".write": "auth !== null && root.child('members/' + $chatID + '/' + auth.uid).exists()",
        ".indexOn": ["text", "timestamp", "fileName"],
        "$messageID": {
          ".validate": "
            newData.hasChildren(['sender', 'text', 'timestamp', 'showTimeAndSender', 'hasBeenEdited']) &&
            (
              newData.child('sender').isString() &&
              newData.child('showTimeAndSender').isBoolean() &&
              newData.child('timestamp').isNumber() &&
              newData.child('text').isString() &&
              newData.child('hasBeenEdited').isBoolean()
            ) &&
            (
              !data.exists() ||
              (
                newData.child('sender').val() === data.child('sender').val() &&
                newData.child('timestamp').val() === data.child('timestamp').val() &&
                newData.child('showTimeAndSender').val() === data.child('showTimeAndSender').val()
              )
            )
          ",
          ".write": "
            (newData.child('sender').val() === auth.uid && !data.exists()) ||
            (data.child('sender').val() === auth.uid && (!newData.exists() || newData.child('text').val() !== null)) ||
            (root.child('chats/' + $chatID + '/owner').val() === auth.uid && !newData.exists())
          "
        }
      }
    },

    "chats": {
      "$chatID": {
        ".read": "auth !== null && root.child('members/' + $chatID + '/' + auth.uid).exists()",
        ".write": "auth !== null && (
          !data.exists() || root.child('chats/' + $chatID + '/owner').val() === auth.uid
        )",
        ".validate": "
          newData.hasChildren(['owner', 'firstMessageID', 'membersTitle', 'title', 'lastMessageTimestamp', 'numOfMembers', 'memberUids']) &&
          (
            newData.child('owner').isString() &&
            newData.child('firstMessageID').isString() &&
            newData.child('membersTitle').isString() &&
            newData.child('title').isString() &&
            newData.child('lastMessageTimestamp').isNumber() &&
            newData.child('numOfMembers').isNumber() &&
            newData.child('memberUids').isString()
          ) &&
          (
            !data.exists() ||
            root.child('chats/' + $chatID + '/owner').val() === auth.uid ||
            (
              newData.child('owner').val() === data.child('owner').val() &&
              newData.child('memberUids').val() === data.child('memberUids').val() &&
              newData.child('numOfMembers').val() === data.child('numOfMembers').val()
            )
          )
        "
      }
    }
  }
}
